# 中綴じPDF再配置アプリ（Booklet Reorder）実装計画

中綴じの書籍や小冊子のホッチキスを外してスキャンしたPDFを、正しいページ順（1, 2, 3...N）に再配置して保存するアプリケーションの設計・実装計画です。

## 1. アプリケーションの形態と技術選定について

ユーザー様のご提案通り、**Webアプリ**の形態が最も汎用性が高く、OS（Windows/Mac）を問わずインストール不要で使えるため非常におすすめです。

**提案する技術スタック**:
- **フロントエンド**: HTML / Vanilla JS / Vanilla CSS (または Vite + React)
- **PDF処理**: `pdf-lib` (ブラウザ上で完全にローカルでPDFを編集・分割・結合できるJavaScriptライブラリ)
- **処理モデル**: サーバーにPDFをアップロードせず、**ユーザーのブラウザ（ローカル環境）内で完結**させます。これにより、機密性の高い個人的な日記などのPDFでも情報漏洩の心配がなく、アップロード・ダウンロードの待ち時間も発生しません。

## 2. 進め方の案（ロードマップ）

以下のステップで進めることを提案します。

1. **要件とアルゴリズムの仕様確定（現在ここ）**
   - どのような順番でスキャンされたか（右開きか左開きか、1ページに2画面収まっているか等）のアルゴリズムを確定します。
2. **UIデザイン案の作成と合意**
   - AIの画像生成ツールを用いてモックアップを作成済み。ユーザーのフィードバックを受け、プレビュー画面を持つ2ステップのUIにブラッシュアップします。
3. **プロジェクトのセットアップと基盤実装**
   - Webアプリの雛形（Viteプロジェクト等）を作成します。
4. **プレビュー機能とPDF処理ロジックの実装**
   - `pdf.js` (または軽量な代替手段) を用いて1ページ目のプレビューを描画します。
   - `pdf-lib` を用いて、指定された回転角度の適用、ページの分割・抽出・並び替えのコアロジックを実装します。
5. **UIへの組み込みと連携**
   - ドラッグ＆ドロップ後、プレビュー画面へ遷移。
   - 90度回転ボタンと「右開き/左開き」選択ボタンを実装。
   - 処理実行とダウンロード機能の実装。
6. **デザインのブラッシュアップ（Polish）**
   - アニメーションやCSSを調整し、リッチで洗練されたUXに仕上げます。
7. **最終確認・完成**

---

## > [!IMPORTANT]
## User Review Required / Open Questions (ユーザー様への確認事項)

実装に進む前に、以下の仕様について教えてください。

### 新たなUIワークフロー（2ステップ）
1. **ステップ1（入力）**: PDFのドラッグ＆ドロップ
2. **ステップ2（調整と実行）**: 
   - 読み込んだPDFの1ページ目（見開き）を画面上にプレビュー表示します。
   - **回転調整**: 90度刻みの回転ボタンで、ユーザーがプレビューを見ながら正しい上下に合わせます。
   - **開き方向指定**: プレビュー上で「1ページ目が右にあるか、左にあるか」を選択するボタンを押し、そのまま処理をスタートします。

これにより、自動判定の不確実性を排除しつつ、直感的に操作できる素晴らしいUXになります。

## Proposed Changes (提案するファイル構成)

### Frontend Components (Vanilla + Viteを想定)
- `index.html`: メインのUI枠組み（ドラッグ＆ドロップエリア、プレビュー画面）
- `src/style.css`: モダンで洗練されたVanilla CSS
- `src/main.js`: イベントリスナーやUIの状態遷移管理
- `src/pdfViewer.js`: プレビュー表示用ロジック（Canvas描画）
- `src/pdfProcessor.js`: `pdf-lib`を用いた回転・分割・並び替えロジック

## Verification Plan (検証計画)

### Automated Tests
- `pdfProcessor.js` に対して、指定したページ数の見開き配列が正しい順番（1, 2, 3...）に計算されるかどうかのユニットテスト（Vitest等を使用）を実行します。

### Manual Verification
- 実際にダミーのPDFをブラウザにドラッグ＆ドロップし、正しい順序でダウンロードできるかを手動で確認します。
